
ul.vertical-menu-container#cntnr
    li
        a.btn-floating.hoverable.waves-effect.waves-light.yellow(@click="addCommentToPDF()" v-if="!selectedDocument.isBeingEdited")
            i.material-icons comment
            //- comment
    li
        a.btn-floating.hoverable.waves-effect.waves-light.red(@click="signDocument()" v-if="!selectedDocument.isBeingEdited && !selectedDocument.isSigned")
            i.material-icons edit 
            //- sign
    //- li
        a.btn-floating.hoverable.waves-effect.waves-light.red
            i.material-icons reply
            //- back
    li
        a.btn-floating.hoverable.waves-effect.waves-light.orange(@click="forwardDocumentModalOpen()" v-if="!selectedDocument.isBeingEdited")
            i.material-icons forward
            //- forward
    li
        a.btn-floating.hoverable.waves-effect.waves-light.green(@click="finalizeTextOnCanvas()" v-if="selectedDocument.isBeingEdited")
            i.material-icons done
            //- save changes
    li
        a.btn-floating.hoverable.waves-effect.waves-light.blue(@click="finalizeDocument()" v-if="!selectedDocument.isBeingEdited && selectedDocument.isSigned")
            i.material-icons done_all
            //- finalize
